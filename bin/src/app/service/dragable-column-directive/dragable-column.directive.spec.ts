import { DraggableColumnDirective } from './draggable-column.directive';

describe('DraggableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
